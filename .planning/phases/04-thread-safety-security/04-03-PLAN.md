---
phase: 4
plan: 03
type: execute
wave: 1
depends_on: ["04-01"]
files_modified: [tomcat/routes/session_routes.py]
autonomous: true
requirements: [SEC-01, SEC-02]
must_haves:
  truths:
    - "Archive extraction is protected against Zip Slip (path traversal)"
    - "Malicious archives abort before any files are written outside target"
  artifacts:
    - path: "tomcat/routes/session_routes.py"
      provides: "Security-hardened import_archive"
      contains: "os.path.realpath"
  key_links:
    - from: "tomcat/routes/session_routes.py"
      to: "os.path.realpath"
      via: "Zip Slip path validation"
---

<objective>
Fix the Zip Slip vulnerability in import_archive to prevent arbitrary file writes outside the target extraction directory.
</objective>

<execution_context>
@/Users/ps/data/wip/tomcat/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/ps/data/wip/tomcat/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-thread-safety-security/04-RESEARCH.md
@tomcat/routes/session_routes.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Zip Slip protection to import_archive</name>
  <files>tomcat/routes/session_routes.py</files>
  <read_first>tomcat/routes/session_routes.py</read_first>
  <behavior>
    - Any archive member with a path that resolves outside the extraction directory results in a ValueError/abort.
    - No files are written outside the sandbox during a malicious import attempt.
  </behavior>
  <action>
    Modify `tomcat/routes/session_routes.py`:
    1. Define a helper function `_safe_extract(tar, dest)` inside `initialize_routes` or at the module level. 
    2. The helper must iterate `tar.getmembers()` and use `os.path.realpath(os.path.join(dest, member.name))` to check if the path starts with `os.path.realpath(dest) + os.sep`.
    3. In the `import_archive` route, call `_safe_extract(tar, temp_dir)` before calling `tar.extractall(path=temp_dir)`.
    4. Handle the error by flashing a message and redirecting back to the upload page.
    5. Ensure that if any member is unsafe, NO members are extracted (abort entire import).
  </action>
  <verify>
    <automated>python -m pytest tests/test_import_archive_security.py</automated>
  </verify>
  <done>Zip Slip protection implemented, security tests pass</done>
  <acceptance_criteria>
    - Grep 'os.path.realpath' tomcat/routes/session_routes.py
    - Grep 'startswith' tomcat/routes/session_routes.py
  </acceptance_criteria>
</task>

</tasks>

<verification>
python -m pytest tests/test_import_archive_security.py
</verification>

<success_criteria>
Security tests pass. Archives with traversal paths are rejected with an error message.
Valid archives continue to work correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/04-thread-safety-security/04-03-SUMMARY.md`
</output>
