---
phase: 4
plan: 02
type: execute
wave: 1
depends_on: ["04-01"]
files_modified: [tomcat/services/media_service.py, tomcat/utils/thread_utils.py]
autonomous: true
requirements: [THREAD-01, THREAD-02, THREAD-03, THREAD-04]
must_haves:
  truths:
    - "Concurrent dict mutations in MediaManager do not raise RuntimeError"
    - "Task submission in ThreadManager is atomic (no duplicate tasks)"
  artifacts:
    - path: "tomcat/services/media_service.py"
      provides: "Thread-safe MediaManager"
      contains: "self._status_lock"
    - path: "tomcat/utils/thread_utils.py"
      provides: "Thread-safe ThreadManager"
      contains: "self._submit_lock"
  key_links:
    - from: "tomcat/services/media_service.py"
      to: "threading.Lock"
      via: "with self._status_lock:"
    - from: "tomcat/utils/thread_utils.py"
      to: "threading.Lock"
      via: "with self._submit_lock:"
---

<objective>
Protect MediaManager dicts and ThreadManager.submit_task with locks to prevent TOCTOU races and RuntimeError during concurrent access.
</objective>

<execution_context>
@/Users/ps/data/wip/tomcat/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/ps/data/wip/tomcat/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-thread-safety-security/04-RESEARCH.md
@tomcat/services/media_service.py
@tomcat/utils/thread_utils.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add locks to MediaManager</name>
  <files>tomcat/services/media_service.py</files>
  <read_first>tomcat/services/media_service.py</read_first>
  <behavior>
    - Multiple threads can read and update media_status, processing_queue, and thumbnail_progress without RuntimeError.
    - All read-modify-write sequences are atomic.
  </behavior>
  <action>
    Modify `tomcat/services/media_service.py`:
    1. Import `threading` at the top.
    2. In `MediaManager.__init__`, initialize three locks: `self._status_lock`, `self._queue_lock`, and `self._progress_lock`.
    3. Use `with self._status_lock:` in `_check_and_generate_lowmag`, `_generate_lowmag_image`, `get_media_status`, etc., to protect all access to `self.media_status`.
    4. Use `with self._queue_lock:` in `queue_tomogram_for_processing` and `process_queue` to protect access to `self.processing_queue`. Capture a snapshot of keys (`list(self.processing_queue.keys())`) inside the lock before processing them outside the lock to minimize contention.
    5. Use `with self._progress_lock:` in `_generate_thumbnail` and `batch_process_tomograms` to protect all access to `self.thumbnail_progress`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_thread_safety.py -k MediaManager</automated>
  </verify>
  <done>Locks added to MediaManager, stress tests pass</done>
  <acceptance_criteria>
    - Grep 'self._status_lock = threading.Lock()' tomcat/services/media_service.py
    - Grep 'with self._queue_lock:' tomcat/services/media_service.py
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add lock to ThreadManager.submit_task</name>
  <files>tomcat/utils/thread_utils.py</files>
  <read_first>tomcat/utils/thread_utils.py</read_first>
  <behavior>
    - If two threads call submit_task simultaneously with the same task_key, only one succeeds.
  </behavior>
  <action>
    Modify `tomcat/utils/thread_utils.py`:
    1. Import `threading` at the top.
    2. In `ThreadManager.__init__`, initialize `self._submit_lock = threading.Lock()`.
    3. In `submit_task()`, wrap the entire check-and-submission block (lines 53-61 approximately) with `with self._submit_lock:`.
    
    This ensures the `if task_key in self.active_futures` check and the `self.thread_pool.submit()` call happen as a single atomic operation.
  </action>
  <verify>
    <automated>python -m pytest tests/test_thread_safety.py -k ThreadManager</automated>
  </verify>
  <done>Locks added to ThreadManager, stress tests pass</done>
  <acceptance_criteria>
    - Grep 'self._submit_lock = threading.Lock()' tomcat/utils/thread_utils.py
    - Grep 'with self._submit_lock:' tomcat/utils/thread_utils.py
  </acceptance_criteria>
</task>

</tasks>

<verification>
python -m pytest tests/test_thread_safety.py
</verification>

<success_criteria>
Existing thread safety tests pass. Concurrent stress tests no longer raise RuntimeError.
No duplicate tasks are submitted in concurrent scenarios.
</success_criteria>

<output>
After completion, create `.planning/phases/04-thread-safety-security/04-02-SUMMARY.md`
</output>
