---
phase: 4
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: [tests/test_thread_safety.py, tests/test_import_archive_security.py]
autonomous: true
requirements: [THREAD-01, THREAD-02, THREAD-03, THREAD-04, SEC-01, SEC-02]
must_haves:
  truths:
    - "Test suite includes failing stress tests for concurrent dict access"
    - "Test suite includes failing stress tests for duplicate task submission"
    - "Test suite includes failing malicious path extraction tests"
  artifacts:
    - path: "tests/test_thread_safety.py"
      provides: "Thread safety verification"
      contains: "TestProcessingQueueLock"
    - path: "tests/test_import_archive_security.py"
      provides: "Zip Slip verification"
      contains: "TestZipSlipValidation"
  key_links:
    - from: "tests/test_thread_safety.py"
      to: "tomcat/services/media_service.py"
      via: "import MediaManager"
    - from: "tests/test_import_archive_security.py"
      to: "tomcat/routes/session_routes.py"
      via: "import session_bp"
---

<objective>
Create test scaffolds for thread safety and security features before implementation (Nyquist Rule).
These tests should initially fail, proving that the concurrency and security issues exist.
</objective>

<execution_context>
@/Users/ps/data/wip/tomcat/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/ps/data/wip/tomcat/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-thread-safety-security/04-RESEARCH.md
@.planning/phases/04-thread-safety-security/04-VALIDATION.md
@tomcat/services/media_service.py
@tomcat/utils/thread_utils.py
@tomcat/routes/session_routes.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create thread safety stress tests</name>
  <files>tests/test_thread_safety.py</files>
  <read_first>tests/test_thread_safety.py</read_first>
  <behavior>
    - Test 1: Concurrent reads/writes to MediaManager.media_status should not raise RuntimeError.
    - Test 2: Concurrent mutations of MediaManager.processing_queue (OrderedDict) should not raise RuntimeError.
    - Test 3: Concurrent updates to MediaManager.thumbnail_progress should not result in inconsistent state (torn reads).
    - Test 4: Simultaneous calls to ThreadManager.submit_task with the same key must return True exactly once and False for the other calls.
  </behavior>
  <action>
    Create a new test file `tests/test_thread_safety.py` containing stress tests for:
    1. `MediaManager.media_status`: Multiple threads reading and writing keys simultaneously.
    2. `MediaManager.processing_queue`: Multiple threads queuing and dequeuing tomograms simultaneously.
    3. `MediaManager.thumbnail_progress`: Multiple threads updating the dict sub-keys in blocks.
    4. `ThreadManager.submit_task`: Use a `threading.Barrier` to synchronize two threads and ensure only one succeeds in submitting a task for the same key.
    
    The tests should NOT pass yet because the locks are not implemented.
  </action>
  <verify>
    <automated>python -m pytest tests/test_thread_safety.py</automated>
  </verify>
  <done>Test file created with failing concurrency tests</done>
  <acceptance_criteria>
    - Grep 'TestProcessingQueueLock' tests/test_thread_safety.py
    - Grep 'TestSubmitTaskAtomicity' tests/test_thread_safety.py
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create archive security tests</name>
  <files>tests/test_import_archive_security.py</files>
  <read_first>tests/test_import_archive_security.py</read_first>
  <behavior>
    - Test 1: An archive with a member named '../../etc/passwd' must be rejected during extraction.
    - Test 2: An archive with a member that is a symlink pointing outside the target directory must be rejected.
    - Test 3: A valid archive with normal paths should be accepted and extracted successfully.
  </behavior>
  <action>
    Create a new test file `tests/test_import_archive_security.py` that:
    1. Generates a malicious tar.gz archive containing path traversal members (`../`).
    2. Tests the (future) validation logic or the `import_archive` route logic to ensure it rejects these paths.
    3. Verifies that no files are written outside the sandbox directory during the attempt.
    4. Verifies a valid archive extracts correctly.
    
    Since the logic isn't there yet, the traversal tests should fail (unless they hit general OS errors).
  </action>
  <verify>
    <automated>python -m pytest tests/test_import_archive_security.py</automated>
  </verify>
  <done>Test file created with failing security tests</done>
  <acceptance_criteria>
    - Grep 'TestZipSlipValidation' tests/test_import_archive_security.py
    - Grep 'test_traversal_aborts_no_write' tests/test_import_archive_security.py
  </acceptance_criteria>
</task>

</tasks>

<verification>
python -m pytest tests/test_thread_safety.py tests/test_import_archive_security.py
</verification>

<success_criteria>
Two new test files exist with comprehensive coverage for Phase 4 requirements.
Tests are failing as expected (demonstrating the bugs/vulnerabilities).
</success_criteria>

<output>
After completion, create `.planning/phases/04-thread-safety-security/04-01-SUMMARY.md`
</output>
